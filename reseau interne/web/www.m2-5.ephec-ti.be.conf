<VirtualHost *:80>
	#Adresse mail de l'administrateur
	ServerAdmin he201832@students.ephec.be
	#Nom du serveur
	ServerName www.m2-5.ephec-ti.be
	#Redirect permanent / https://www.m2-5.ephec-ti.be
	DocumentRoot /var/www/www.m2-5.ephec-ti.be/

	ErrorLog ${APACHE_LOG_DIR}/error.log              
	CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>

<VirtualHost *:443>
	#Adresse mail de l'administrateur
	ServerAdmin he201832@students.ephec.be
	#Nom du serveur
	ServerName www.m2-5.ephec-ti.be
	DocumentRoot /var/www/www.m2-5.ephec-ti.be/

	ErrorLog ${APACHE_LOG_DIR}/error.log              
	CustomLog ${APACHE_LOG_DIR}/access.log combined
	
	# SSLEngine on
	# SSLCertificateFile /etc/ssl/certs/www.crt
	# SSLCertificateKeyFile /etc/ssl/certs/www.key
</VirtualHost>